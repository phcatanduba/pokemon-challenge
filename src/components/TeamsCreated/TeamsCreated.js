import Header from "../Header";
import Teams from "./Teams";

export default function TeamsCreated() {
  return (
    <main>
      <Header text={"CREATE NEW TEAM"} />
      <Teams />
    </main>
  );
}
